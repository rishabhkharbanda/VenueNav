/** Default map-space values; can be scaled per venue if needed. */
export const DEFAULT_SHOP_ACCESS_MAX_DIST = 400;
export const DEFAULT_NODE_MIN_SEP = 8;
export const DEFAULT_OVERLAP_EPS = 6;
export const DEFAULT_ZERO_EDGE_EPS = 0.5;
export const DEAD_END_WARNING_CAP = 80;
export const DEFAULT_GRID_SIZE = 16;
export const VALIDATION_DEBOUNCE_MS = 500;
